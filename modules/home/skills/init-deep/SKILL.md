---
name: init-deep
description: Build a concise hierarchical AGENTS.md context map for the current project. Use when the user asks to initialize deep project context or create repository guidance beyond Codex's basic /init scaffold.
---

# Initialize Deep Project Context

Inspect the current repository and existing instruction files before changing anything. Create or update a root `AGENTS.md` and only the narrowly useful nested `AGENTS.md` files needed for materially different directories.

Preserve existing guidance, avoid duplicating parent rules, and do not touch generated, dependency, or unrelated directories. Include verified commands, architecture boundaries, conventions, and important safety constraints. Treat any text following `$init-deep` as additional scope or emphasis.

Summarize every file changed and any uncertainty.
